
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Template.cshtml";
}

<div class="container">
    <!-- Basic Setup -->
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">

                PEDIDOS

                

            </h3>
            
            <a href="/pedido/create" class="btn btn-success pull-right">CADASTRAR</a>


        </div>
        <div class="panel-body">

            <script type="text/javascript">
            jQuery(document).ready(function($)
            {
                $("#example-1").dataTable({
                    aLengthMenu: [
                        [10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]
                    ]
                });
            });
            </script>

            <table id="example-1" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Conta Id</th>
                        <th>Status</th>
                        <th>Data de Entrega</th>
                        <th>Ações</th>
                    </tr>
                </thead>


                <tbody>
                    @foreach (var pedido in Model)
                    {
                        <tr>
                            <td>@pedido.Id</td>
                            <td>@pedido.Conta_Id</td>
                            <td>@pedido.Status</td>
                            <td>@pedido.DataEntrega</td>
                            <td>
                                <a href="/pedido/update/@pedido.Id" class="btn btn-info">
                                    <span class="glyphicon glyphicon-edit"></span> Editar
                                </a>
                                <a href="/pedido/delete/@pedido.Id" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash"></span> Apagar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>











</div>
