
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Template.cshtml";
}

<div class="container">
    <!-- Basic Setup -->
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">
                FUNCIONÁRIOS
                
            </h3>
            <a href="/funcionario/create" class="btn btn-success pull-right">CADASTRAR</a>
        </div>
        <div class="panel-body">
            <script type="text/javascript">
            jQuery(document).ready(function($)
            {
                $("#example-1").dataTable({
                    aLengthMenu: [
                        [10, 25, 50, 100, -1], [10, 25, 50, 100, "All"]
                    ]
                });
            });
            </script>
            <table id="example-1" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nome</th>
                        <th>Senha</th>
                        <th>CPF</th>
                        <th>Hora Entrada</th>
                        <th>Hora Saída</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var funcionario in Model)
                    {
                        <tr>
                            <td>@funcionario.Id</td>
                            <td>@funcionario.Nome</td>
                            <td>@funcionario.Senha</td>
                            <td>@funcionario.Cpf</td>
                            <td>@funcionario.HorarioEntrada</td>
                            <td>@funcionario.HorarioSaida</td>
                            <td>
                                <a href="/funcionario/update/@funcionario.Id" class="btn btn-info">
                                    <span class="glyphicon glyphicon-edit"></span> Editar
                                </a>
                                <a href="/funcionario/delete/@funcionario.Id" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash"></span> Apagar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>







</div>
